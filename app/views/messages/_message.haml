%div{:id => "#{dom_id(message)}_messages"}
  - if message.user?
    .bg-sky-400.rounded-lg.m-8.text-green.p-4
      = message.content
  - else
    .bg-gray-200.rounded-lg.m-8.p-4
      = message.content
